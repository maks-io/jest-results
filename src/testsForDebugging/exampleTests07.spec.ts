describe("Example Tests 07 (with complexer comparisons)", () => {
  it("Tests something (07 - A)", () => {
    expect([1234, 5678]).toEqual(["a"]);
  });
  it.skip("Tests something, but is manually skipped (07 - B)", () => {
    expect([1234, 5678]).toEqual(["a"]);
  });
});
