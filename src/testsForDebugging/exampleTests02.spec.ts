describe("Example Tests 02", () => {
  it("Tests something (02 - A)", () => {
    expect(true).toBeTruthy();
  });
  it("Tests something (02 - B)", () => {
    expect(true).toBeTruthy();
  });
  it("Tests something (02 - C)", () => {
    expect(false).toBeTruthy(); // fails on purpose
  });
});
